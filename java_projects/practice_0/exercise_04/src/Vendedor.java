import java.util.Random;
public class Vendedor {
/**
    //01. ATTRIBUTES
    private int[] notas;

    // 02. CONSTRUCTOR
    public Vendedor(int[] notas) {
        this.notas = notas;
    }

 **/
    public int[] generarNotas(int numeroVendedor){
        int diasMes = 30;
        int randomNumber = random.nextInt(4) + 1;
        int[] notas = new int[diasMes];
        for(int i = 0; i < diasMes; i++){ // para cada día del mes
            Nota nota = new Nota(numeroVendedor,);

            notas[i] = ;
        }
        return notas;

}
